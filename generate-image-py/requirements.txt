# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.30.0
diffusers>=0.21.0
accelerate>=0.20.0
peft>=0.7.0  # Required for LoRA support

# Web framework
Flask>=2.3.0
waitress>=2.1.0

# Image processing
Pillow>=9.5.0
opencv-python>=4.8.0

# Configuration and data handling
PyYAML>=6.0
numpy>=1.24.0

# Hugging Face integration
huggingface-hub>=0.16.0

# Optional: for better performance
safetensors>=0.3.0
xformers>=0.0.20  # Optional: for memory efficiency

# Development and testing (optional)
pytest>=7.0.0
requests>=2.31.0

# Logging and monitoring
psutil>=5.9.0  # For system monitoring

# GGUF support (if needed)
gguf>=0.1.0  # For GGUF model format support

# Additional ML libraries
scipy>=1.10.0
scikit-image>=0.20.0

# For better JSON handling
orjson>=3.9.0  # Optional: faster JSON processing

# Memory management
gc-python-utils>=0.0.1  # Optional: better garbage collection

# CUDA utilities (if available)
nvidia-ml-py>=11.495.46  # Optional: NVIDIA GPU monitoring

# Production server
gunicorn>=20.1.0  # Alternative to waitress

# Configuration validation
cerberus>=1.3.4  # Optional: for config validation

# Async support (if needed in future)
aiohttp>=3.8.0  # Optional: for async HTTP
aiofiles>=23.0.0  # Optional: for async file operations